set -g mouse on
# Start windows and panes at 1, not 0
set -g base-index 1
setw -g pane-base-index 1
source-file ~/.config/tmux/rose-pine-dawn.tmux

# switch to this if you start using tmux windows more
# bind-key -n M-q selectw -t 1
# bind-key -n M-w selectw -t 2
# bind-key -n M-e selectw -t 3
# bind-key -n M-r selectw -t 4
# bind-key -n M-t selectw -t 5
# bind-key -n M-y selectw -t 6

#otherwise this works well for now
bind-key -n M-n select-window -n
bind-key -n M-p select-window -p
# TODO: fix previous window not working, next works fine for some reason
# bind-key -n M-p select-window -p
